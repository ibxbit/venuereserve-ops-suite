{
  "name": "fitness-wellness-studio-platform",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev:backend": "npm run dev --workspace backend",
    "dev:frontend": "npm run dev --workspace frontend",
    "migrate": "npm run migrate --workspace backend",
    "test": "npm run test --workspace backend"
  }
}
